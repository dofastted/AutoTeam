# 邮箱提供者
MAIL_PROVIDER=mo_email

# Mo Email
MO_EMAIL_BASE_URL=https://mo.gymbro.cloud
MO_EMAIL_API_KEY=your_api_key
MO_EMAIL_DOMAIN=gymbro.cloud
MO_EMAIL_NAME_PREFIX=abc
MO_EMAIL_START_INDEX=1
MO_EMAIL_EXPIRY_TIME=3600000

# CloudMail
CLOUDMAIL_BASE_URL=https://example.com/api
CLOUDMAIL_EMAIL=admin@example.com
CLOUDMAIL_PASSWORD=your_password
CLOUDMAIL_DOMAIN=@example.com

# Cloudflare Temp Email
CF_TEMP_EMAIL_BASE_URL=https://tempmail.example.com
CF_TEMP_EMAIL_ADMIN_PASSWORD=your_admin_password
CF_TEMP_EMAIL_DOMAIN=example.com

# 远端同步目标开关（建议显式设置，避免占位值被误判为已启用）
SYNC_TARGET_CPA=false
SYNC_TARGET_SUB2API=false

# CPA (CLIProxyAPI)
CPA_URL=http://127.0.0.1:8317
CPA_KEY=your_key

# Sub2API
SUB2API_URL=http://127.0.0.1:8080
SUB2API_EMAIL=admin@example.com
SUB2API_PASSWORD=your_password
SUB2API_GROUP=

# API 鉴权（不设置则不启用，任何人都可访问）
API_KEY=

# Playwright 浏览器运行（hidden 不弹窗，visible 显示窗口，embedded 预留给页面内嵌模式）
PLAYWRIGHT_BROWSER_MODE=hidden
# 兼容旧配置；新配置优先使用 PLAYWRIGHT_BROWSER_MODE
PLAYWRIGHT_HEADLESS=true
# 账号补满、轮转和直注批量任务的并行窗口数，范围 1-3
BROWSER_PARALLEL_WORKERS=1

# 全局出口代理池（默认 Windows Clash）
# 后端访问 OpenAI/ChatGPT、邮箱服务、CPA、Sub2API 时使用；本地地址默认绕过
OUTBOUND_PROXY_ENABLED=true
# 多个代理可用英文逗号或换行分隔；支持 http/https/socks5/socks5h，也支持 direct/none
OUTBOUND_PROXY_POOL=http://127.0.0.1:10808
OUTBOUND_PROXY_BYPASS=localhost,127.0.0.1,::1
# 当前实现为任务内固定代理
OUTBOUND_PROXY_STRATEGY=task-sticky
OUTBOUND_PROXY_FAILOVER=true

# Playwright 浏览器代理（可选）
# 留空时跟随上面的全局出口代理池；只有浏览器需要单独代理时才填写
# 示例：socks5://host.docker.internal:1080
# 注意：Playwright/Chromium 不支持带认证的 socks5（如 socks5://user:pass@host:port）
PLAYWRIGHT_PROXY_URL=
# 示例：localhost,127.0.0.1
PLAYWRIGHT_PROXY_BYPASS=

# 轮询配置
EMAIL_POLL_INTERVAL=3
# 设置为 0 时一直等待
EMAIL_POLL_TIMEOUT=300

# 自动巡检（API 模式下生效）
AUTO_CHECK_INTERVAL=300      # 巡检间隔（秒），默认 5 分钟
AUTO_CHECK_THRESHOLD=10      # 额度低于此百分比触发轮转，默认 10%
AUTO_CHECK_MIN_LOW=2         # 至少几个账号低于阈值才触发，默认 2

# Team 人数目标与单次补满数量
TEAM_TARGET_SEATS=999
FILL_BATCH_SIZE=10
